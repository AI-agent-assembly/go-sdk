version: "2"

run:
  timeout: 3m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - revive

issues:
  exclude-rules:
    - path: assembly/tool_wrapper.go
      linters:
        - revive
      text: stutters; consider calling this Tool
